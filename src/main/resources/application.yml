spring:
  application:
    name: stock-service
server:
  port: 8302
  
  
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8080/eureka/
  instance:
    hostname: localhost